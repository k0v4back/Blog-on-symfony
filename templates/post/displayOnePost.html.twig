{% extends 'home.html.twig' %}

{% block body %}
    <div class="col-md-8">
        <h1 class="my-4">
        </h1>
        <h2>{{ post.title }}</h2>
        <p>{{ post.content| raw}}</p>
    </div>
{% endblock %}

